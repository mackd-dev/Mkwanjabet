# Local PostgreSQL (matches docker-compose.yml)
DATABASE_URL="postgresql://primeodds:primeodds_dev_password@localhost:5434/primeodds?schema=public"

# Use a long random value in production (minimum 32 characters)
JWT_SECRET="replace-this-with-a-long-random-secret-at-least-32-characters"

# Frontend origin; used later for CORS/webhooks
NEXT_PUBLIC_APP_URL="http://localhost:3000"
