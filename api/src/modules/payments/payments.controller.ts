import { Body, Controller, Get, Post, UseGuards } from "@nestjs/common"; import { IsEnum, IsString, Matches } from "class-validator"; import { PaymentProvider } from "@prisma/client"; import { PrismaService } from "../../prisma/prisma.service"; import { CurrentUser } from "../auth/current-user.decorator"; import { JwtAuthGuard } from "../auth/jwt-auth.guard";
class InitiatePaymentDto{@IsString()planId:string;@IsEnum(PaymentProvider)provider:PaymentProvider;@Matches(/^\+?[1-9]\d{8,14}$/)phone:string}
@UseGuards(JwtAuthGuard) @Controller("payments") export class PaymentsController{constructor(private db:PrismaService){} @Get("me")list(@CurrentUser()u:{id:string}){return this.db.payment.findMany({where:{userId:u.id},include:{plan:true},orderBy:{createdAt:"desc"}})} @Post("initiate")async initiate(@CurrentUser()u:{id:string},@Body()d:InitiatePaymentDto){const plan=await this.db.plan.findUniqueOrThrow({where:{id:d.planId}});return this.db.payment.create({data:{userId:u.id,planId:plan.id,provider:d.provider,phone:d.phone,amountTzs:plan.priceTzs,status:"PENDING"}})}}
