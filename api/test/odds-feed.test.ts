import assert from "node:assert/strict";import test from "node:test";import {adjustedPrice,outcomeKey,slug,sportName,uniqueCode} from "../src/modules/odds-feed/odds-feed.service";test("maps provider outcomes",()=>{const event={home_team:"Arsenal",away_team:"Chelsea"};assert.equal(outcomeKey("Arsenal",event),"home");assert.equal(outcomeKey("Chelsea",event),"away");assert.equal(outcomeKey("Draw",event),"draw")});test("normalizes provider values",()=>{assert.equal(slug("Soccer EPL"),"soccer-epl");assert.equal(uniqueCode("soccer_epl"),"ODDS_SOCCER_EPL");assert.equal(sportName("soccer_epl"),"Football");assert.equal(adjustedPrice(2.345,1),2.34);assert.equal(adjustedPrice(1.005,.95),1.01)});
